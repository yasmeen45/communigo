package ui;

import model.Activity;
import model.User;

import java.awt.*;

public class Communigo {

    private User mainUser;
    private List upcomingActivities;

    public Communigo() {
        runApp();
    }

    // MODIFIES: this
    private void runApp() {
        // welcome! ask for name
        // mainUser = new User(name);
        // display menu
    }

    /*
    private void displayMenu() {
        list options
        take user input + handle errors then go to function depending on option chosen
    } */

    /*
    private void viewActivitiesJoined() {
        get activitiesJoined from user class
        ask how they would like to filter them -> go to appropriate filter function
        display result after filtering
        list actions (back to main menu, filter again)
    } */

    /*
    private void displayActivities(List<Activity>) {
        display all activities in given list
    } */

    /*
    private Boolean?? handleUserInput(String input, List acceptedValues) {
        if input not an accepted value {
            ask for valid input;
            return true
        }
    } */
}
